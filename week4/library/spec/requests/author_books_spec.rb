require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/author_books", type: :request do
  
  # This should return the minimal set of attributes required to create a valid
  # AuthorBook. As you add validations to AuthorBook, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      AuthorBook.create! valid_attributes
      get author_books_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      author_book = AuthorBook.create! valid_attributes
      get author_book_url(author_book)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_author_book_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      author_book = AuthorBook.create! valid_attributes
      get edit_author_book_url(author_book)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new AuthorBook" do
        expect {
          post author_books_url, params: { author_book: valid_attributes }
        }.to change(AuthorBook, :count).by(1)
      end

      it "redirects to the created author_book" do
        post author_books_url, params: { author_book: valid_attributes }
        expect(response).to redirect_to(author_book_url(AuthorBook.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new AuthorBook" do
        expect {
          post author_books_url, params: { author_book: invalid_attributes }
        }.to change(AuthorBook, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post author_books_url, params: { author_book: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested author_book" do
        author_book = AuthorBook.create! valid_attributes
        patch author_book_url(author_book), params: { author_book: new_attributes }
        author_book.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the author_book" do
        author_book = AuthorBook.create! valid_attributes
        patch author_book_url(author_book), params: { author_book: new_attributes }
        author_book.reload
        expect(response).to redirect_to(author_book_url(author_book))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        author_book = AuthorBook.create! valid_attributes
        patch author_book_url(author_book), params: { author_book: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested author_book" do
      author_book = AuthorBook.create! valid_attributes
      expect {
        delete author_book_url(author_book)
      }.to change(AuthorBook, :count).by(-1)
    end

    it "redirects to the author_books list" do
      author_book = AuthorBook.create! valid_attributes
      delete author_book_url(author_book)
      expect(response).to redirect_to(author_books_url)
    end
  end
end
